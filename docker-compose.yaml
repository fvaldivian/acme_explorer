version: '3.7'

services:
  mongo:
    image: mongo:5.0
    ports:
      - '27017:27017'
    volumes:
      - mongodbdata:/data/db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=user
      - MONGO_INITDB_ROOT_PASSWORD=S3cret
      - MONGO_INITDB_DATABASE=acme_explorer

volumes:
  mongodbdata:
